// Copy this file to environment.ts and fill in real values.
// environment.ts is git-ignored — never commit real keys.

export const environment = {
  production: false,
  // Get a free key at https://openrouter.ai/keys
  openRouterKey: 'sk-or-v1-REPLACE_ME',
};
